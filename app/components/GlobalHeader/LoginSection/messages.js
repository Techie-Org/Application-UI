/**
 * LoginSection messages
 *
 * This contains all the text for LoginSection component
 */
import { defineMessages } from 'react-intl';

const scope = 'artistry.components.GlobalHeader.LoginSection';

export default defineMessages({
  logIn: {
    id: `${scope}.logIn`,
    defaultMessage: 'Log In',
  },
  signUp: {
    id: `${scope}.signUp`,
    defaultMessage: 'Sign Up',
  },
});
